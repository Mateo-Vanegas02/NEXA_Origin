spring.application.name=MotvirsWeb
motvirs.model.path=modelo_satisfaccion_j48.model

# Configuración principal de MongoDB
# El URI completo se carga desde una variable de entorno
spring.data.mongodb.uri=${MONGO_URI}

logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=TRACE

# La URL base puede cambiar entre desarrollo y producción
app.base-url=${APP_BASE_URL}

# Configuración de correo
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# El usuario y contraseña se cargan desde variables de entorno
spring.mail.username=${GMAIL_USER}
spring.mail.password=${GMAIL_PASSWORD}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

debug=true